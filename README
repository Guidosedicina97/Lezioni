Librerie installate:
- Pandas (creazione e gestione di dataframe) --> pip install pandas
- Matplotlib (creazione di grafici di base) --> pip install matplotlib
- Scipy (calcoli scientifici e statistici) --> pip install scipy